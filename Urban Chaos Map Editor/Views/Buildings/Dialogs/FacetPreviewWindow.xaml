<Window x:Class="UrbanChaosMapEditor.Views.Buildings.Dialogs.FacetPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Facet Editor" Width="960" Height="680"
        Background="#202225"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="False"
        ResizeMode="CanResize">

    <Window.Resources>
        <SolidColorBrush x:Key="Fg" Color="White"/>
        <SolidColorBrush x:Key="Subtle" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="EditableBg" Color="#2A2D32"/>
        <SolidColorBrush x:Key="EditableBorder" Color="#4A4D52"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="ContentPresenter">
            <Setter Property="TextElement.Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="Border">
            <Setter Property="BorderBrush" Value="#333"/>
        </Style>

        <Style x:Key="EditableField" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource EditableBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource EditableBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Subtle}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3D42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="WhiteCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="260" MaxWidth="380"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Properties -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Text="Facet Editor" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <!-- Identity -->
                <Border BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Identity" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="FacetIdText" Margin="0,6,0,0"/>
                        <TextBlock x:Name="FacetTypeText" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Coordinates -->
                <Border BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Coordinates" FontWeight="Bold" FontSize="14"/>
                        <StackPanel x:Name="CoordPanel" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Height -->
                <Border BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Height" FontWeight="Bold" FontSize="14"/>
                        <StackPanel x:Name="HeightPanel" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Flags -->
                <Border BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Flags" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="FacetFlagsText" Margin="0,6,0,0" TextWrapping="Wrap"/>
                        <ItemsControl x:Name="FlagsList" Margin="0,6,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource WhiteCheckBox}"
                                        Content="{Binding Name}"
                                        IsChecked="{Binding IsSet, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Tag="{Binding Bit}" Margin="0,2,12,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Style section (Face A / primary) -->
                <Border BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock x:Name="StyleSectionLabel" Text="Style" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="StyleModeText" Margin="0,6,0,0"/>
                        <TextBlock x:Name="RecipeText" Margin="0,4,0,0" Foreground="{StaticResource Subtle}"/>

                        <StackPanel x:Name="RawStylePanel" Margin="0,6,0,0">
                            <TextBlock x:Name="RawStyleText"/>
                        </StackPanel>

                        <StackPanel x:Name="PaintedPanel" Margin="0,6,0,0" Visibility="Collapsed">
                            <TextBlock x:Name="PaintedSummaryText"/>
                            <TextBlock Text="Paint bytes (raw):" Margin="0,6,0,2" FontWeight="SemiBold"/>
                            <ScrollViewer Height="70" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="PaintBytesHexText" FontFamily="Consolas" TextWrapping="Wrap"/>
                            </ScrollViewer>

                            <TextBlock Text="Decoded per-byte:" Margin="0,10,0,4" FontWeight="SemiBold"/>
                            <DataGrid x:Name="PaintBytesGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Height="160">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding Index}" Header="#" Width="40"/>
                                    <DataGridTextColumn Binding="{Binding ByteHex}" Header="Byte" Width="70"/>
                                    <DataGridTextColumn Binding="{Binding Page}" Header="Page" Width="100"/>
                                    <DataGridTextColumn Binding="{Binding Flag}" Header="Flag" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                        <!-- Paint Facet Button -->
                        <Button x:Name="BtnPaintFacet" 
                                Content="?? Paint Facet..." 
                                Margin="0,10,0,0"
                                Style="{StaticResource ActionButton}"
                                Click="BtnPaintFacet_Click"
                                ToolTip="Open the texture painter to paint individual cells of this facet."/>
                        <Button x:Name="BtnChangeStyle"
                                Content="??? Change Style..."
                                Margin="0,8,0,0"
                                Style="{StaticResource ActionButton}"
                                Click="BtnChangeStyle_Click"
                                ToolTip="Select a new raw TMA style for this facet."/>
                    </StackPanel>
                </Border>

                <!-- Face B / Second Face Style (only visible for TwoTextured / TwoSided) -->
                <Border x:Name="SecondFacePanel" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="SecondFaceSectionLabel" Text="Face B Style (Interior)" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="SecondFaceStyleModeText" Margin="0,6,0,0"/>
                        <TextBlock x:Name="SecondFaceRecipeText" Margin="0,4,0,0" Foreground="{StaticResource Subtle}"/>

                        <StackPanel x:Name="SecondFaceRawPanel" Margin="0,6,0,0">
                            <TextBlock x:Name="SecondFaceRawStyleText"/>
                        </StackPanel>

                        <StackPanel x:Name="SecondFacePaintedPanel" Margin="0,6,0,0" Visibility="Collapsed">
                            <TextBlock x:Name="SecondFacePaintedSummaryText"/>
                            <TextBlock Text="Paint bytes (raw):" Margin="0,6,0,2" FontWeight="SemiBold"/>
                            <ScrollViewer Height="70" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="SecondFacePaintHexText" FontFamily="Consolas" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </StackPanel>

                        <Button x:Name="BtnChangeSecondFaceStyle"
                                Content="??? Change Face B Style..."
                                Margin="0,8,0,0"
                                Style="{StaticResource ActionButton}"
                                Click="BtnChangeSecondFaceStyle_Click"
                                ToolTip="Select a new TMA style for the second face (interior/back)."/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Tip: Use 'Redraw on Map' to interactively reposition the facet. First click = start, second click = end. Right-click cancels."
                           Foreground="{StaticResource Subtle}" TextWrapping="Wrap" FontSize="11"/>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Column="1"/>

        <!-- RIGHT: Visual preview (Face A on top, Face B below if dual-face) -->
        <Border Grid.Column="2" BorderThickness="1" CornerRadius="6" Padding="8">
            <ScrollViewer x:Name="PreviewScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#1E1F22">
                <StackPanel>
                    <!-- Face A label (only shown when dual-face) -->
                    <TextBlock x:Name="FaceALabel" Text="? Face A (Outside)" FontWeight="SemiBold" Margin="0,0,0,4" Visibility="Collapsed"/>
                    <Grid>
                        <Canvas x:Name="PanelCanvas" Background="#202225" SnapsToDevicePixels="True" ClipToBounds="True"/>
                        <Canvas x:Name="GridCanvas" IsHitTestVisible="False"/>
                    </Grid>

                    <!-- Face B preview (only shown for TwoTextured / TwoSided) -->
                    <TextBlock x:Name="FaceBLabel" Text="? Face B (Interior)" FontWeight="SemiBold" Margin="0,12,0,4" Visibility="Collapsed"/>
                    <Grid x:Name="FaceBPreviewGrid" Visibility="Collapsed">
                        <Canvas x:Name="FaceBPanelCanvas" Background="#202225" SnapsToDevicePixels="True" ClipToBounds="True"/>
                        <Canvas x:Name="FaceBGridCanvas" IsHitTestVisible="False"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
