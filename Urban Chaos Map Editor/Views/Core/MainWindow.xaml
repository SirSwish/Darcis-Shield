<Window x:Class="UrbanChaosMapEditor.Views.Core.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:UrbanChaosEditor.Shared.Converters;assembly=UrbanChaosEditor.Shared"
        xmlns:views="clr-namespace:UrbanChaosMapEditor.Views.Core"
        xmlns:bldTab="clr-namespace:UrbanChaosMapEditor.Views.Buildings"
        xmlns:hgtTab="clr-namespace:UrbanChaosMapEditor.Views.Heights"
        xmlns:prmTab="clr-namespace:UrbanChaosMapEditor.Views.Prims"
        xmlns:texTab="clr-namespace:UrbanChaosMapEditor.Views.Textures"
        xmlns:rfTab="clr-namespace:UrbanChaosMapEditor.Views.Roofs"
        mc:Ignorable="d"
        Title="{Binding Title}"
        WindowStartupLocation="CenterScreen"
        Width="1280" Height="860"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="#1E1E1E">

    <Window.Resources>
        <conv:NullToBoolConverter x:Key="NullToBool" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <SolidColorBrush x:Key="BackgroundDark" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BackgroundMedium" Color="#252526"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3E3E42"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TextMuted" Color="#888888"/>
        <SolidColorBrush x:Key="AccentColor" Color="#861b2d"/>
        <SolidColorBrush x:Key="AccentColorLight" Color="#a62b3f"/>
        <SolidColorBrush x:Key="AccentColorDark" Color="#660000"/>

        <Style x:Key="LayerToggleStyle" TargetType="ToggleButton">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#861b2d"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#a62b3f"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#888"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4C4C4C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#861b2d"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+O" Command="{Binding LoadMapCommand}" />
        <KeyBinding Gesture="Ctrl+S" Command="{Binding SaveMapCommand}" />
        <KeyBinding Gesture="Ctrl+Shift+S" Command="{Binding SaveAsMapCommand}" />
        <KeyBinding Gesture="Ctrl+N" Command="{Binding NewMapCommand}" />
        <KeyBinding Gesture="Ctrl+G" Command="{Binding ToggleGridLinesCommand}" />
        <KeyBinding Gesture="Ctrl+B" Command="{Binding ToggleBuildingsCommand}" />
        <KeyBinding Gesture="Ctrl+W" Command="{Binding ToggleWalkablesCommand}" />
        <KeyBinding Gesture="Ctrl+M" Command="{Binding ToggleMapWhoCommand}" />
        <KeyBinding Gesture="Ctrl+0" Command="{Binding ResetZoomCommand}" />
        <KeyBinding Gesture="Ctrl+H" Command="{Binding ToggleHeightsCommand}" />
        <KeyBinding Gesture="Ctrl+T" Command="{Binding ToggleTexturesCommand}" />
        <KeyBinding Gesture="Ctrl+J" Command="{Binding ToggleObjectsCommand}" />
        <KeyBinding Gesture="Ctrl+Y" Command="{Binding TogglePrimGraphicsCommand}" />
        <KeyBinding Key="Delete" Command="{Binding DeletePrimCommand}" />
    </Window.InputBindings>

    <DockPanel LastChildFill="True">

        <Menu DockPanel.Dock="Top" Background="#2D2D2D" Foreground="#FFFFFF">
            <MenuItem Header="_File">
                <MenuItem Header="_New Map" Command="{Binding NewMapCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open Map..." Command="{Binding LoadMapCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save Map" Command="{Binding SaveMapCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save Map _As..." Command="{Binding SaveAsMapCommand}" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem Header="_Recent" SubmenuOpened="Recent_SubmenuOpened">
                    <Separator Visibility="Collapsed"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}" InputGestureText="Alt+F4"/>
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="_Raise Height" Command="{Binding Map.RaiseHeightCommand}"/>
                <MenuItem Header="_Lower Height" Command="{Binding Map.LowerHeightCommand}"/>
                <MenuItem Header="_Level (click-drag)" Command="{Binding Map.LevelHeightCommand}"/>
                <MenuItem Header="_Flatten Height (set 0)" Command="{Binding Map.FlattenHeightCommand}"/>
                <Separator/>
                <MenuItem Header="_Copy Object" InputGestureText="Ctrl+C" Click="CopyPrim_Click"/>
                <MenuItem Header="_Paste Object" InputGestureText="Ctrl+V" Click="PastePrim_Click"/>
                <Separator/>
                <MenuItem Header="Copy _Cells" InputGestureText="Ctrl+C"
                          Command="{Binding Map.CopyTextureCellsCommand}"
                          ToolTip="Copy selected texture area to clipboard (activate via Select Area in the Textures tab)"/>
                <MenuItem Header="Paste C_ells" InputGestureText="Ctrl+V"
                          Command="{Binding Map.PasteTextureCellsCommand}"
                          ToolTip="Paste copied texture cells — click on the map to place"/>
                <MenuItem Header="_Delete Selected Object"
                          Command="{Binding DeletePrimCommand}"
                          InputGestureText="Delete"
                          IsEnabled="{Binding Map.SelectedPrim, Converter={StaticResource NullToBool}}"/>
                <MenuItem Header="Object _Height..."
                          Click="PrimHeight_Click"
                          IsEnabled="{Binding Map.SelectedPrim, Converter={StaticResource NullToBool}}"/>
            </MenuItem>

            <MenuItem Header="_View">
                <MenuItem Header="Zoom _In" Command="{Binding ZoomInCommand}" InputGestureText="+"/>
                <MenuItem Header="Zoom _Out" Command="{Binding ZoomOutCommand}" InputGestureText="-"/>
                <MenuItem Header="_Reset Zoom" Command="{Binding ResetZoomCommand}" InputGestureText="Ctrl+0"/>
                <MenuItem Header="_Go to Cell..." Click="GoToCell_Click"/>
                <Separator/>
                <MenuItem Header="Map Layers">
                    <MenuItem Header="Map" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Grid Lines" IsCheckable="True" IsChecked="{Binding Map.ShowGridLines}" InputGestureText="Ctrl+G"/>
                    <MenuItem Header="Show _MapWho" IsCheckable="True" IsChecked="{Binding Map.ShowMapWho}" InputGestureText="Ctrl+M"/>
                    <Separator/>
                    <MenuItem Header="Textures" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Textures" IsCheckable="True" IsChecked="{Binding Map.ShowTextures}" InputGestureText="Ctrl+T"/>
                    <Separator/>
                    <MenuItem Header="Terrain" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Heights" IsCheckable="True" IsChecked="{Binding Map.ShowHeights}" InputGestureText="Ctrl+H"/>
                    <MenuItem Header="Show _PAP Flags" IsCheckable="True" IsChecked="{Binding Map.ShowCellFlags}"/>
                    <Separator/>
                    <MenuItem Header="Prims" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Objects" IsCheckable="True" IsChecked="{Binding Map.ShowObjects}" InputGestureText="Ctrl+J"/>
                    <MenuItem Header="Show Object _Graphics" IsCheckable="True" IsChecked="{Binding Map.ShowPrimGraphics}" InputGestureText="Ctrl+Y"/>
                    <Separator/>
                    <MenuItem Header="Buildings" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Buildings" IsCheckable="True" IsChecked="{Binding Map.ShowBuildings}" InputGestureText="Ctrl+B"/>
                    <Separator/>
                    <MenuItem Header="Roofs" IsEnabled="False" FontWeight="SemiBold"/>
                    <MenuItem Header="Show _Walkables" IsCheckable="True" IsChecked="{Binding Map.ShowWalkables}" InputGestureText="Ctrl+W"/>
                    <MenuItem Header="Show Roof _Tiles" IsCheckable="True" IsChecked="{Binding Map.ShowRoofs}"/>
                    <MenuItem Header="Show Roof _Altitudes" IsCheckable="True" IsChecked="{Binding Map.ShowRoofAltitudes}"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Use Beta Textures" IsCheckable="True" IsChecked="{Binding Map.UseBetaTextures, Mode=TwoWay}"/>
            </MenuItem>

            <MenuItem Header="_Diagnostics">
                <MenuItem Header="Diagnose _Walkables..." Click="DiagnoseWalkables_Click"/>
                <Separator/>
                <MenuItem Header="Dump _Indoor Data..." Click="DumpIndoorData_Click"
          ToolTip="Dump all InsideStorey, Staircase, and InsideBlock data"/>
                <Separator/>
                <MenuItem Header="?? _Indoor Building Editor..."
                          Click="IndoorBuildingEditor_Click"
                          ToolTip="View and edit DStorey, dstyles, and other indoor building components"/>
                <Separator/>
                <MenuItem Header="Snapshot Building Block..." Click="SnapshotBuildingBlock_Click"/>
                <Separator/>
                <MenuItem Header="Dump All Buildings Raw..." Click="DumpAllBuildingsRaw_Click"/>
                <MenuItem Header="Dump Selected _Building Raw Bytes..." Click="DumpBuildingBytes_Click"/>
                <MenuItem Header="Dump Selected _Facet Raw Bytes..." Click="DumpFacetBytes_Click"/>
                <MenuItem Header="Dump Building _Walkables + RoofFace4..." Click="DumpBuildingWalkables_Click"/>
                <MenuItem Header="Dump _Walkable Raw Bytes..."
                          Click="DumpWalkableRawBytes_Click"
                          ToolTip="Dump raw 22-byte structure for a specific walkable (for comparison)"/>
                <MenuItem Header="Dump _InsideStorey for Building..."
                          Click="DumpInsideStoreyForBuilding_Click"
                          ToolTip="Dump InsideStorey entries linked to a building (for roof rendering analysis)"/>
                <Separator/>
                <MenuItem Header="Dump _PAP_HI Cell Info..." Click="DumpPapHiCell_Click"/>
                <Separator/>
                <MenuItem Header="_Edit Walkable Points..."
                          Click="EditWalkablePoints_Click"
                          ToolTip="Modify StartPoint/EndPoint values for testing roof rendering"/>
                <MenuItem Header="Set _RF4 DrawFlags..."
                          Click="SetRF4DrawFlags_Click"
                          ToolTip="Change DrawFlags for all RoofFace4 entries in a walkable (test 0x00 vs 0x08)"/>
                <MenuItem Header="?? _Paint RF4 Tiles..."
                          Click="PaintRF4Tiles_Click"
                          ToolTip="Visual grid editor for painting individual roof tiles"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="Help Documentation" Click="Help_Click"/>
                <Separator/>
                <MenuItem Header="_About" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <Border DockPanel.Dock="Top" Background="#2D2D2D" Padding="4">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled"
                          CanContentScroll="False">
                <StackPanel Orientation="Horizontal">

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Style="{StaticResource ToolbarButtonStyle}" Content="📂 Open"
                                Command="{Binding LoadMapCommand}" ToolTip="Open Map File (Ctrl+O)"/>
                        <Button Style="{StaticResource ToolbarButtonStyle}" Content="💾 Save"
                                Command="{Binding SaveMapCommand}" ToolTip="Save Map File (Ctrl+S)"/>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Map" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Grid"
                                          IsChecked="{Binding Map.ShowGridLines}" ToolTip="Ctrl+G"/>
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="MapWho"
                                          IsChecked="{Binding Map.ShowMapWho}" ToolTip="Ctrl+M"/>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Textures" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Textures"
                                          IsChecked="{Binding Map.ShowTextures}" ToolTip="Ctrl+T"/>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Terrain" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Heights"
                                          IsChecked="{Binding Map.ShowHeights}" ToolTip="Ctrl+H"/>
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="PAP Flags"
                                          IsChecked="{Binding Map.ShowCellFlags}" ToolTip="Show PAP_HI cell flags"/>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Prims" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Objects"
                                          IsChecked="{Binding Map.ShowObjects}" ToolTip="Ctrl+J"/>
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Graphics"
                                          IsChecked="{Binding Map.ShowPrimGraphics}" ToolTip="Ctrl+Y"/>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Buildings" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Buildings"
                                          IsChecked="{Binding Map.ShowBuildings}" ToolTip="Ctrl+B"/>
                        </StackPanel>
                    </StackPanel>

                    <Rectangle Width="1" Fill="#555" Margin="10,4" VerticalAlignment="Stretch"/>

                    <StackPanel Orientation="Vertical" Margin="4,0">
                        <TextBlock Text="Roofs" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Walkables"
                                          IsChecked="{Binding Map.ShowWalkables}" ToolTip="Ctrl+W"/>
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Roof Tiles"
                                          IsChecked="{Binding Map.ShowRoofs}" ToolTip="Ctrl+R"/>
                            <ToggleButton Style="{StaticResource LayerToggleStyle}" Content="Altitudes"
                                          IsChecked="{Binding Map.ShowRoofAltitudes}" ToolTip="Show cell altitudes for walkable regions"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border DockPanel.Dock="Bottom"
                Height="24"
                Background="#861b2d"
                BorderThickness="0,1,0,0"
                BorderBrush="#660000">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           Margin="8,0"
                           Foreground="White"
                           FontSize="12"/>

                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontSize="12"
                           Margin="8,0">
                    <Run Text="Map: "/>
                    <Run Text="{Binding StatusFiles, Mode=OneWay}"/>
                </TextBlock>

                <TextBlock Grid.Column="2"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontSize="12"
                           Margin="8,0">
                    <Run Text="Zoom: "/>
                    <Run Text="{Binding Map.Zoom, StringFormat=P0, Mode=OneWay}"/>
                </TextBlock>

                <ProgressBar Grid.Column="3"
                             Width="80"
                             Height="6"
                             Margin="8,0"
                             VerticalAlignment="Center"
                             IsIndeterminate="True"
                             Foreground="White"
                             Background="#770000"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="EditorRailCol" Width="28"/>
                <ColumnDefinition x:Name="EditorDrawerCol"
                                  Width="400"
                                  MinWidth="400"
                                  MaxWidth="600"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Expander x:Name="EditorExpander"
                      Grid.Column="0"
                      IsExpanded="True"
                      ExpandDirection="Right"
                      Background="#1E1E1E"
                      Foreground="White"
                      BorderBrush="#3E3E42"
                      BorderThickness="0,0,1,0"
                      Expanded="EditorExpander_Expanded"
                      Collapsed="EditorExpander_Collapsed"
                      Grid.ColumnSpan="2">

                <Expander.Header>
                    <TextBlock Text="Editor"
                               FontWeight="SemiBold"
                               Padding="4"
                               Foreground="White">
                        <TextBlock.LayoutTransform>
                            <RotateTransform Angle="-90"/>
                        </TextBlock.LayoutTransform>
                    </TextBlock>
                </Expander.Header>

                <Grid Background="#252526" VerticalAlignment="Stretch">
                    <TabControl x:Name="EditorTabControl"
                                Background="#252526"
                                Foreground="White"
                                SelectedIndex="0"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True"
                                SelectionChanged="EditorTabs_SelectionChanged">

                        <TabControl.ItemContainerStyle>
                            <Style TargetType="TabItem">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#2D2D2D"/>
                                <Setter Property="BorderBrush" Value="#3E3E42"/>
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="MinWidth" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="1,1,1,0"
                                                    Padding="{TemplateBinding Padding}"
                                                    Margin="{TemplateBinding Margin}"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter ContentSource="Header"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#1E1E1E"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#861b2d"/>
                                                    <Setter TargetName="border" Property="BorderThickness" Value="1,2,1,0"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#3C3C3C"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.ItemContainerStyle>

                        <TabControl.Template>
                            <ControlTemplate TargetType="TabControl">
                                <Grid KeyboardNavigation.TabNavigation="Local"
                                      SnapsToDevicePixels="True">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <UniformGrid Grid.Row="0"
                                                 Rows="1"
                                                 Columns="5"
                                                 IsItemsHost="True"
                                                 Background="#252526"/>

                                    <Border Grid.Row="1"
                                            Background="#252526"
                                            BorderBrush="#3E3E42"
                                            BorderThickness="1">
                                        <ContentPresenter x:Name="PART_SelectedContentHost"
                                                          Margin="0"
                                                          ContentSource="SelectedContent"
                                                          HorizontalAlignment="Stretch"
                                                          VerticalAlignment="Stretch"/>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </TabControl.Template>

                        <TabItem Header="Textures">
                            <texTab:TexturesTab/>
                        </TabItem>

                        <TabItem Header="Heights">
                            <hgtTab:HeightsTab/>
                        </TabItem>

                        <TabItem Header="Prims">
                            <prmTab:PrimsTab/>
                        </TabItem>

                        <TabItem Header="Buildings">
                            <bldTab:BuildingsTab/>
                        </TabItem>

                        <TabItem Header="Roofs">
                            <rfTab:RoofsTab/>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Expander>

            <GridSplitter Grid.Column="2"
                          Width="6"
                          HorizontalAlignment="Stretch"
                          Background="#22000000"
                          Cursor="SizeWE"
                          ShowsPreview="True"
                          ResizeDirection="Columns"
                          ResizeBehavior="PreviousAndNext"
                          Visibility="{Binding IsExpanded, ElementName=EditorExpander, Converter={StaticResource BoolToVis}}"/>

            <views:MapView x:Name="MapViewControl"
                           Grid.Column="3"
                           DataContext="{Binding Map}"/>
        </Grid>

    </DockPanel>
</Window>