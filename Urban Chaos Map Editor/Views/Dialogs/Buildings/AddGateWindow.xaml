<Window x:Class="UrbanChaosMapEditor.Views.Dialogs.Buildings.AddGateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Gate" 
        Width="400"
        SizeToContent="Height"
        Background="#202225"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="Fg" Color="White"/>
        <SolidColorBrush x:Key="Subtle" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="Accent" Color="#5865F2"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2F3136"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3D42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4D52"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2D5A27"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D7A37"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DrawButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#5865F2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6875F3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Subtle}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Add Gate to Fence" 
                       FontWeight="Bold" 
                       FontSize="16"/>
            <TextBlock x:Name="TxtBuildingInfo"
                       Text="Building #0"
                       Foreground="{StaticResource Subtle}"
                       FontSize="12"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Coordinates -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Gate Coordinates (draw on map or enter manually):" Style="{StaticResource FieldLabel}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="X0" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Z0" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="4" Text="X1" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="6" Text="Z1" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>

                <TextBox Grid.Row="2" Grid.Column="0" x:Name="TxtX0" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="2" x:Name="TxtZ0" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="4" x:Name="TxtX1" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="6" x:Name="TxtZ1" Text="0"/>

                <Button Grid.Row="2" Grid.Column="8" 
                        x:Name="BtnDrawOnMap"
                        Content="Draw on Map"
                        Style="{StaticResource DrawButton}"
                        Click="BtnDrawOnMap_Click"
                        ToolTip="Click to draw the gate segment on the map"/>
            </Grid>
        </StackPanel>

        <!-- Gate Type -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="Gate Height:" Style="{StaticResource FieldLabel}"/>
            <ComboBox x:Name="CmbGateType" SelectedIndex="1">
                <ComboBoxItem Content="Low (64 units)" Tag="1"/>
                <ComboBoxItem Content="Standard Fence (128 units)" Tag="2"/>
                <ComboBoxItem Content="Tall Security (192 units)" Tag="3"/>
            </ComboBox>
        </StackPanel>

        <!-- Style -->
        <StackPanel Grid.Row="6">
            <TextBlock Text="Texture Style ID:" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="TxtStyleId" Text="22"/>
            <TextBlock Text="Use same style as adjacent fence for consistency"
                       Foreground="{StaticResource Subtle}"
                       FontSize="10"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Options -->
        <StackPanel Grid.Row="8">
            <TextBlock Text="Options:" Style="{StaticResource FieldLabel}"/>
            <CheckBox x:Name="ChkElectrified" Content="Electrified (damages on touch)"/>
            <CheckBox x:Name="ChkBarbedTop" Content="Barbed Wire Top"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="10" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Cancel" 
                    Style="{StaticResource ActionButton}"
                    Click="BtnCancel_Click"
                    Margin="0,0,8,0"/>
            <Button x:Name="BtnAdd"
                    Content="Add Gate" 
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnAdd_Click"/>
        </StackPanel>
    </Grid>
</Window>
