<Window x:Class="UrbanChaosMapEditor.Views.Roofs.Dialogs.AddRoofFace4Dialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Roof Face" 
        Width="380"
        SizeToContent="Height"
        Background="#202225"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="Fg" Color="White"/>
        <SolidColorBrush x:Key="Subtle" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="Accent" Color="#5865F2"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2F3136"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3D42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4D52"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2D5A27"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D7A37"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Subtle}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Add Roof Face to Walkable" 
                       FontWeight="Bold" 
                       FontSize="16"/>
            <TextBlock x:Name="TxtWalkableInfo"
                       Text="Walkable #0 (Building #0)"
                       Foreground="{StaticResource Subtle}"
                       FontSize="12"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Roof Type Selection -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Roof Type:" Style="{StaticResource FieldLabel}"/>
            <StackPanel Margin="8,0,0,0">
                <RadioButton x:Name="RbFlatRoof" 
                             Content="Fill entire Walkable" 
                             IsChecked="True"
                             Checked="RoofType_Changed"/>
                <RadioButton x:Name="RbSingleTile" 
                             Content="Add Single Tile" 
                             Checked="RoofType_Changed"/>
            </StackPanel>
        </StackPanel>

        <!-- Common Settings -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Altitude (Y):" Style="{StaticResource FieldLabel}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="TxtAltitude" Text="512" Width="80"/>
                    <TextBlock Text="256 = 1 floor"
                               Foreground="{StaticResource Subtle}"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Single Tile Settings -->
        <StackPanel Grid.Row="5" x:Name="PnlSingleTileSettings" Visibility="Collapsed" Margin="0,16,0,0">
            <TextBlock Text="Tile Position &amp; Corners:" Style="{StaticResource FieldLabel}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="RX" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="RZ" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="4" Text="DY0 (SW)" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="6" Text="DY1 (NW)" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="8" Text="DY2 (NE)" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>

                <TextBox Grid.Row="2" Grid.Column="0" x:Name="TxtRX" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="2" x:Name="TxtRZ" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="4" x:Name="TxtDY0" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="6" x:Name="TxtDY1" Text="0"/>
                <TextBox Grid.Row="2" Grid.Column="8" x:Name="TxtDY2" Text="0"/>
            </Grid>

            <CheckBox x:Name="ChkWalkable"
                      Content="Walkable (0x08)"
                      IsChecked="True"
                      Margin="0,8,0,0"/>

            <TextBlock Text="Corner Layout: Y=SE (base), DY0=SW, DY1=NW, DY2=NE"
                       Foreground="{StaticResource Subtle}"
                       FontSize="10"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel" 
                    Style="{StaticResource ActionButton}"
                    Click="BtnCancel_Click"
                    Margin="0,0,8,0"/>
            <Button x:Name="BtnAdd"
                    Content="Add Flat Roof" 
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnAdd_Click"/>
        </StackPanel>
    </Grid>
</Window>