<UserControl x:Class="UrbanChaosMapEditor.Views.Textures.TexturesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UrbanChaosMapEditor.Views.Textures">

    <UserControl.Resources>
        <local:TextureSelectionConverter x:Key="TextureSelectionConverter"/>
        <local:RotationArrowConverter x:Key="RotationArrowConverter"/>
    </UserControl.Resources>

    <!-- Inherit DataContext from MainWindow (do NOT set DataContext here) -->

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Browser -->
        <TabControl Grid.Row="0">
            <!-- WORLD -->
            <TabItem Header="World">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Map.WorldTextures}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" ItemWidth="50" ItemHeight="50"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="Auto" Height="Auto"
                                        Margin="4" Padding="0"
                                        Click="TextureThumb_Click"
                                        Tag="{Binding}">
                                    <Border Background="#222"
                                            BorderThickness="1"
                                            BorderBrush="#444">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#222"/>
                                                <Setter Property="BorderBrush" Value="#444"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="Effect" Value="{x:Null}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource TextureSelectionConverter}">
                                                                <Binding/>
                                                                <Binding Path="DataContext.Map.SelectedTextureGroup"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                                <Binding Path="DataContext.Map.SelectedTextureNumber"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <Setter Property="BorderBrush" Value="Yellow"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="16"
                                                                                  ShadowDepth="0"
                                                                                  Color="Yellow"
                                                                                  Opacity="1"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Image Source="{Binding Image}"
                                               Stretch="UniformToFill"
                                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <!-- SHARED -->
            <TabItem Header="Shared">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Map.SharedTextures}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" ItemWidth="50" ItemHeight="50"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="Auto" Height="Auto"
                                        Margin="4" Padding="0"
                                        Click="TextureThumb_Click"
                                        Tag="{Binding}">
                                    <Border Background="#222"
                                            BorderThickness="1"
                                            BorderBrush="#444">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#222"/>
                                                <Setter Property="BorderBrush" Value="#444"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="Effect" Value="{x:Null}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource TextureSelectionConverter}">
                                                                <Binding/>
                                                                <Binding Path="DataContext.Map.SelectedTextureGroup"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                                <Binding Path="DataContext.Map.SelectedTextureNumber"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <Setter Property="BorderBrush" Value="Yellow"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="16"
                                                                                  ShadowDepth="0"
                                                                                  Color="Yellow"
                                                                                  Opacity="1"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Image Source="{Binding Image}"
                                               Stretch="UniformToFill"
                                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <!-- PRIMS -->
            <TabItem Header="Prims">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Map.PrimTextures}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" ItemWidth="50" ItemHeight="50"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="Auto" Height="Auto"
                                        Margin="4" Padding="0"
                                        Click="TextureThumb_Click"
                                        Tag="{Binding}">
                                    <Border Background="#222"
                                            BorderThickness="1"
                                            BorderBrush="#444">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#222"/>
                                                <Setter Property="BorderBrush" Value="#444"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="Effect" Value="{x:Null}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource TextureSelectionConverter}">
                                                                <Binding/>
                                                                <Binding Path="DataContext.Map.SelectedTextureGroup"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                                <Binding Path="DataContext.Map.SelectedTextureNumber"
                                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <Setter Property="BorderBrush" Value="Yellow"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="16"
                                                                                  ShadowDepth="0"
                                                                                  Color="Yellow"
                                                                                  Opacity="1"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Image Source="{Binding Image}"
                                               Stretch="UniformToFill"
                                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Bottom controls -->
        <Border Grid.Row="1" Background="#2b2b2b" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="TextWrapping" Value="Wrap"/>
                    </Style>
                    <Style TargetType="CheckBox">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#333333"/>
                        <Setter Property="BorderBrush" Value="#444444"/>
                    </Style>
                    <Style TargetType="Button">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </Grid.Resources>

                <!-- World + Set -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,6">
                    <TextBlock Text="World:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox Width="25"
                             Text="{Binding Map.TextureWorld, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalContentAlignment="Right"
                             VerticalContentAlignment="Center"
                             PreviewTextInput="Units_PreviewTextInput"
                             DataObject.Pasting="Units_OnPaste"/>
                    <CheckBox Margin="8,0,0,0"
                              Content="Use Beta Textures"
                              IsChecked="{Binding Map.UseBetaTextures, Mode=TwoWay}"/>
                </StackPanel>

                <!-- Rotation + Brush -->
                <WrapPanel Grid.Row="1" VerticalAlignment="Top" ItemHeight="28" ItemWidth="Auto">
                    <TextBlock Text="Rotation:" VerticalAlignment="Center" Margin="0,0,8,0"/>

                    <Button Width="28" Height="24" Click="RotateLeft_Click" ToolTip="Rotate CCW (-90)"
                            Background="#DDDDDD" BorderBrush="#666666">
                        <TextBlock Text="&lt;" FontSize="14" Foreground="#111111"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                    <Button Width="28" Height="24" Margin="6,0,0,0"
                            Click="RotateRight_Click" ToolTip="Rotate CW (+90)"
                            Background="#DDDDDD" BorderBrush="#666666">
                        <TextBlock Text="&gt;" FontSize="14" Foreground="#111111"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                    <!-- Replaces the old helper text -->
                    <Border Margin="10,0,0,0"
                            Width="34"
                            Height="24"
                            Background="#333333"
                            BorderBrush="#555555"
                            BorderThickness="1"
                            CornerRadius="3">
                        <TextBlock Text="{Binding Map.SelectedRotationIndex, Converter={StaticResource RotationArrowConverter}}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center"/>
                    </Border>

                    <TextBlock Text="   Brush:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                    <Slider Width="100" Height="22"
                            Minimum="1" Maximum="10"
                            TickFrequency="1"
                            IsSnapToTickEnabled="True"
                            Value="{Binding Map.BrushSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="{Binding Map.BrushSize}"
                               VerticalAlignment="Center"
                               Margin="6,0,0,0"
                               Width="22"
                               TextAlignment="Right"/>
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.85"
                               Text="(for click / Shift+drag)"/>
                </WrapPanel>

                <!-- Tips -->
                <StackPanel Grid.Row="2" Margin="0,8,0,0">
                    <TextBlock Opacity="0.7" FontSize="11" TextWrapping="Wrap">
                        <Run FontWeight="SemiBold">Click</Run><Run>: Paint with brush size</Run>
                        <Run Text="  |  "/>
                        <Run FontWeight="SemiBold">Drag</Run><Run>: Rectangle fill</Run>
                        <Run Text="  |  "/>
                        <Run FontWeight="SemiBold">Shift+Drag</Run><Run>: Stroke paint</Run>
                        <Run Text="  |  "/>
                        <Run FontWeight="SemiBold">Space</Run><Run>: Rotate</Run>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>