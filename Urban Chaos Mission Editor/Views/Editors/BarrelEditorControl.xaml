<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.BarrelEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Barrel Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Barrel Type -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Type:"/>
            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding BarrelTypeOptions}"
                      SelectedIndex="{Binding BarrelType}"
                      ToolTip="The type of barrel/object to spawn"/>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Info text -->
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="Barrels and similar objects spawn at this position when triggered."/>
            <LineBreak/><LineBreak/>
            <Run Text="- Barrel/Bin - Can be knocked over or destroyed"/>
            <LineBreak/>
            <Run Text="- Traffic cone - Small moveable obstacle"/>
            <LineBreak/>
            <Run Text="- Burning variants - On fire, can spread flames"/>
            <LineBreak/>
            <Run Text="- Oil/LOX drum - Explosive when damaged"/>
        </TextBlock>
    </StackPanel>
</UserControl>
