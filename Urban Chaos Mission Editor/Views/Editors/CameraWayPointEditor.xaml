<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.CameraWaypointEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,4"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Camera Waypoint Settings:" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Move Type -->
            <Label Grid.Row="0" Grid.Column="0" Content="Move Type:"/>
            <ComboBox Grid.Row="0" Grid.Column="1" 
                      ItemsSource="{Binding CameraMoveTypeOptions}"
                      SelectedIndex="{Binding CameraMoveType}"/>

            <!-- Dwell Time -->
            <Label Grid.Row="1" Grid.Column="0" Content="Dwell Time:"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding DwellTime, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Time (in game ticks) the camera stays at this waypoint"/>
        </Grid>

        <Separator Margin="0,15,0,10" Background="#FF555555"/>

        <!-- Move Type Descriptions -->
        <TextBlock Text="Move Types:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontSize="11">
            <Run FontWeight="SemiBold">Normal:</Run> Standard camera movement
            <LineBreak/>
            <Run FontWeight="SemiBold">Smooth:</Run> Smooth interpolated movement
            <LineBreak/>
            <Run FontWeight="SemiBold">Wobbly:</Run> Shaky handheld-style movement
        </TextBlock>

        <Separator Margin="0,10,0,10" Background="#FF555555"/>

        <!-- Info text -->
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic">
            Camera waypoints define a path for the camera to follow. Link multiple waypoints 
            together using the EP Reference field to create camera paths. The dwell time 
            specifies how long the camera pauses at this waypoint before moving to the next.
        </TextBlock>
    </StackPanel>
</UserControl>
