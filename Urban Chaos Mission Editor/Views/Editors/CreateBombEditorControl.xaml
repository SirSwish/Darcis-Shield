<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.CreateBombEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Create Bomb Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Bomb Type -->
        <TextBlock Text="Bomb Appearance" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Bomb Type:"/>
            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding BombTypeOptions}"
                      SelectedIndex="{Binding BombType}"
                      ToolTip="Visual appearance of the bomb"/>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Bomb Size -->
        <TextBlock Text="Explosion Settings" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Size:"/>
            <Slider Grid.Column="1" 
                    Minimum="0" Maximum="1024" 
                    Value="{Binding BombSize}"
                    TickFrequency="64"
                    IsSnapToTickEnabled="False"
                    ToolTip="Explosion radius (0-1024)"/>
            <TextBox Grid.Column="2" 
                     Text="{Binding BombSize, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Explosion radius (0-1024)"/>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Visual Effects -->
        <TextBlock Text="Visual Effects" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock Text="Select which effects play during the explosion:" 
                   Foreground="#FF888888" FontStyle="Italic" FontSize="11" Margin="0,0,0,5"/>
        <StackPanel Margin="10,0,0,10">
            <CheckBox Content="Flare (bright flash)" 
                      IsChecked="{Binding BombFxFlare}"
                      ToolTip="Creates a bright flash effect"/>
            <CheckBox Content="Fire Dome (expanding fireball)" 
                      IsChecked="{Binding BombFxFireDome}"
                      ToolTip="Creates an expanding dome of fire"/>
            <CheckBox Content="Shockwave (expanding ring)" 
                      IsChecked="{Binding BombFxShockwave}"
                      ToolTip="Creates an expanding shockwave ring"/>
            <CheckBox Content="Smoke Trails (rising smoke)" 
                      IsChecked="{Binding BombFxSmokeTrails}"
                      ToolTip="Creates trails of smoke"/>
            <CheckBox Content="Bonfire (persistent fire)" 
                      IsChecked="{Binding BombFxBonfire}"
                      ToolTip="Creates persistent flames"/>
        </StackPanel>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Info -->
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="Creates a bomb that can be triggered to explode."/>
            <LineBreak/>
            <Run Text="The bomb type determines its visual appearance before detonation."/>
            <LineBreak/>
            <Run Text="Size controls the explosion radius, and effects determine the visual feedback."/>
        </TextBlock>
    </StackPanel>
</UserControl>
