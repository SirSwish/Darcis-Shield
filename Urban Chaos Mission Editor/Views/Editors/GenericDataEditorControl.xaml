<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.GenericDataEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Raw Data Values:" FontWeight="SemiBold" Margin="0,0,0,10"/>
        <TextBlock Text="(No specific editor available for this waypoint type)" 
                   Foreground="#FF888888" FontStyle="Italic" Margin="0,0,0,10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Data[0]:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Data0, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="0" Grid.Column="2" Content="Data[1]:"/>
            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Data1, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Data[2]:"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Data2, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="1" Grid.Column="2" Content="Data[3]:"/>
            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Data3, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Data[4]:"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Data4, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="2" Grid.Column="2" Content="Data[5]:"/>
            <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Data5, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Data[6]:"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Data6, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="3" Grid.Column="2" Content="Data[7]:"/>
            <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding Data7, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Data[8]:"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Data8, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="4" Grid.Column="2" Content="Data[9]:"/>
            <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding Data9, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </StackPanel>
</UserControl>
