<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.ItemEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,4,10,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Item Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Basic Settings -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Item Type:"/>
            <ComboBox Grid.Row="0" Grid.Column="1" 
                      ItemsSource="{Binding ItemTypeOptions}"
                      SelectedIndex="{Binding ItemType}"
                      ToolTip="The type of item to spawn"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Count:"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding ItemCount, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Number of items to spawn (1-99)"/>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Item Flags -->
        <TextBlock Text="Flags:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <StackPanel>
            <CheckBox Content="Item follows person" 
                      IsChecked="{Binding ItemFlagFollowsPerson}"
                      ToolTip="Item is attached to and follows a person"/>
            <CheckBox Content="Item hidden in prim" 
                      IsChecked="{Binding ItemFlagHiddenInPrim}"
                      ToolTip="Item is hidden inside a map object (prim)"/>
        </StackPanel>

        <Separator Background="#FF555555" Margin="0,10,0,10"/>

        <!-- Info text -->
        <TextBlock Text="Items spawn at this position when triggered. Use 'hidden in prim' for items that should be discovered by interacting with objects."
                   TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11"/>
    </StackPanel>
</UserControl>
