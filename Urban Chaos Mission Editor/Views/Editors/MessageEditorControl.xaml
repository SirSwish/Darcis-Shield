<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.MessageEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Message Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Speaker and Duration -->
        <TextBlock Text="Display Settings" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Speaker:"/>
            <ComboBox Grid.Row="0" Grid.Column="1" 
                      ItemsSource="{Binding MessageSpeakerOptions}"
                      SelectedIndex="{Binding MessageSpeakerIndex}"
                      ToolTip="Who speaks this message"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Speaker EP:"
                   Visibility="{Binding MessageSpeakerEPVisibility}"/>
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal"
                        Visibility="{Binding MessageSpeakerEPVisibility}">
                <TextBox Text="{Binding MessageSpeakerEP, UpdateSourceTrigger=PropertyChanged}"
                         Width="80" HorizontalAlignment="Left"
                         ToolTip="EventPoint index of the character who speaks"/>
                <TextBlock Text="(player/enemy EP index)" Foreground="#FF888888" 
                           VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic"/>
            </StackPanel>

            <Label Grid.Row="2" Grid.Column="0" Content="Duration:"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <TextBox Text="{Binding MessageTime, UpdateSourceTrigger=PropertyChanged}"
                         Width="80" HorizontalAlignment="Left"
                         ToolTip="How long the message displays (in seconds)"/>
                <TextBlock Text="seconds" Foreground="#FF888888" 
                           VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic"/>
            </StackPanel>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Message Text -->
        <TextBlock Text="Message Text" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock Text="Enter the message text below (max 2550 characters):" 
                   Foreground="#FF888888" FontStyle="Italic" FontSize="11" Margin="0,0,0,5"/>
        <TextBox Text="{Binding ExtraText, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="120"
                 MaxLength="2550"
                 ToolTip="The message text to display"/>

        <Separator Background="#FF555555" Margin="0,10,0,10"/>

        <!-- Speaker Type Info -->
        <TextBlock Text="Speaker Types:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="- Radio - Message appears as radio communication"/>
            <LineBreak/>
            <Run Text="- Place/Street Name - Displays as location identifier"/>
            <LineBreak/>
            <Run Text="- Tutorial Help - Shows as help/tutorial text"/>
            <LineBreak/>
            <Run Text="- Character EP - Spoken by specific player or enemy"/>
        </TextBlock>
    </StackPanel>
</UserControl>
