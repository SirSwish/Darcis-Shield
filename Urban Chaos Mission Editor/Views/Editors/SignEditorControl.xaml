<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.SignEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <TextBlock Text="Sign Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <!-- Sign Type -->
            <TextBlock Text="Sign Type" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Type:"/>
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding SignTypeOptions}"
                          SelectedIndex="{Binding SignType}"
                          ToolTip="Data[0] - Sign type (0..3)"/>
            </Grid>

            <Separator Background="#FF555555" Margin="0,5,0,10"/>

            <!-- Flip Flags -->
            <TextBlock Text="Flip" FontWeight="SemiBold" Margin="0,0,0,5"/>

            <StackPanel>
                <CheckBox Content="Flip Left / Right"
                          IsChecked="{Binding SignFlipLeftRight}"
                          ToolTip="Data[1] bit 1"/>
                <CheckBox Content="Flip Top / Bottom"
                          IsChecked="{Binding SignFlipTopBottom}"
                          ToolTip="Data[1] bit 2"/>
            </StackPanel>

            <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11" Margin="0,10,0,0">
                <Run Text="From signsetup.cpp:"/>
                <LineBreak/>
                <Run Text="Data[0] = sign type, Data[1] = flip flags (1=LR, 2=TB)."/>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>