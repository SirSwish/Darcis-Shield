<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.SimpleWaypointEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Simple Waypoint Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Delay -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Delay:"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBox Text="{Binding SimpleWaypointDelay, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Delay in tenths of a second (0-10000)"/>
                <TextBlock Text="{Binding SimpleWaypointDelaySeconds, StringFormat='= {0:F1} seconds'}" 
                           Foreground="#FF888888" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Info -->
        <TextBlock Text="About Simple Waypoints:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="Simple Waypoints are generic location markers used for:"/>
            <LineBreak/><LineBreak/>
            <Run Text="- Patrol routes for enemies and vehicles"/>
            <LineBreak/>
            <Run Text="- Path nodes for movement sequences"/>
            <LineBreak/>
            <Run Text="- Intermediate trigger points"/>
            <LineBreak/>
            <Run Text="- Target destinations for other EventPoints"/>
            <LineBreak/><LineBreak/>
            <Run Text="The delay value specifies how long (in tenths of a second) an entity"/>
            <Run Text="should pause at this waypoint before continuing. Set to 0 for no delay."/>
        </TextBlock>
    </StackPanel>
</UserControl>
