<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.SoundEffectEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Height" Value="260"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <TextBlock Text="Sound Effect Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <!-- Type -->
            <TextBlock Text="Type" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Category:"/>
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding SfxTypeOptions}"
                          SelectedIndex="{Binding SfxType}"
                          ToolTip="Data[0] - 0=Sound FX, 1=Music"/>
            </Grid>

            <Separator Background="#FF555555" Margin="0,5,0,10"/>

            <!-- Sound list -->
            <TextBlock Text="Sound" FontWeight="SemiBold" Margin="0,0,0,5"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <ListBox Grid.Column="0"
                         ItemsSource="{Binding SfxSoundOptions}"
                         SelectedIndex="{Binding SfxSoundSelectedIndex}"
                         ToolTip="Filtered from sound_list[]; selection writes absolute sound_list index to Data[1]"/>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Label Content="Sound ID:"/>
                    <TextBox Text="{Binding SfxId, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Data[1] - absolute index into sound_list[]"/>
                </StackPanel>
            </Grid>

            <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11" Margin="0,8,0,0">
                <Run Text="Matches original editor:"/>
                <LineBreak/>
                <Run Text="Data[0] = type (Sound FX / Music), Data[1] = index into sound_list[]."/>
                <LineBreak/>
                <Run Text="Filter rule matches C: case-sensitive contains &quot;music&quot;."/>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>