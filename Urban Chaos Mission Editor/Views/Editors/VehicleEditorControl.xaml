<UserControl x:Class="UrbanChaosMissionEditor.Views.Editors.VehicleEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Vehicle Configuration" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Vehicle Type -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Type:"/>
            <ComboBox Grid.Row="0" Grid.Column="1" 
                      ItemsSource="{Binding VehicleTypeOptions}"
                      SelectedIndex="{Binding VehicleType}"
                      ToolTip="The type of vehicle to create"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Behaviour:"/>
            <ComboBox Grid.Row="1" Grid.Column="1" 
                      ItemsSource="{Binding VehicleBehaviorOptions}"
                      SelectedIndex="{Binding VehicleBehavior}"
                      ToolTip="How the vehicle behaves when spawned"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Key Required:"/>
            <ComboBox Grid.Row="2" Grid.Column="1" 
                      ItemsSource="{Binding VehicleKeyOptions}"
                      SelectedIndex="{Binding VehicleKey}"
                      ToolTip="Key required to enter the vehicle"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Target EP:"/>
            <TextBox Grid.Row="3" Grid.Column="1" 
                     Text="{Binding VehicleTarget, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="EventPoint index to track (only used with 'Track Target' behaviour)"
                     IsEnabled="{Binding IsVehicleTargetEnabled}"/>
        </Grid>

        <Separator Background="#FF555555" Margin="0,5,0,10"/>

        <!-- Behaviour descriptions -->
        <TextBlock Text="Behaviour Descriptions:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="- Player Drives - Player can enter and drive this vehicle"/>
            <LineBreak/>
            <Run Text="- Patrol - Vehicle follows patrol waypoints"/>
            <LineBreak/>
            <Run Text="- Guard - Vehicle guards an area"/>
            <LineBreak/>
            <Run Text="- Track Target - Vehicle tracks the specified EventPoint (player/enemy)"/>
        </TextBlock>

        <Separator Background="#FF555555" Margin="0,10,0,10"/>

        <!-- Key descriptions -->
        <TextBlock Text="Key Types:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock TextWrapping="Wrap" Foreground="#FF888888" FontStyle="Italic" FontSize="11">
            <Run Text="- Unlocked - No key required"/>
            <LineBreak/>
            <Run Text="- Red/Blue/Green/Black/White - Requires matching coloured key item"/>
            <LineBreak/>
            <Run Text="- Locked - Cannot be entered by player"/>
        </TextBlock>
    </StackPanel>
</UserControl>
 